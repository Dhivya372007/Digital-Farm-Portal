import { X } from 'lucide-react';
export default function Modal({ open, title, onClose, children }) { if (!open) return null; return <div className="fixed inset-0 z-40 flex items-center justify-center bg-gray-900/40 p-6 transition-opacity duration-200"><div role="dialog" aria-modal="true" className="w-full max-w-xl rounded-2xl bg-white p-8 shadow-xl transition-transform duration-200"><div className="mb-6 flex items-center justify-between"><h2 className="text-xl font-semibold text-text-primary">{title}</h2><button onClick={onClose} aria-label="Close modal" className="rounded-xl p-2 text-text-secondary hover:bg-gray-100"><X className="h-5 w-5"/></button></div>{children}</div></div>; }
